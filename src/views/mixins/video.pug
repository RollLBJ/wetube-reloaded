mixin video(video)
  div
    h4
      a(href=`/videos/${video.id}`)=video.title
    p=video.description
    ul 
      each hashtag in video.hashtags
        li=hashtag
    small=video.createdAt
    form(method="POST")
      button(name="deleteId" value=video.id) Delete
    hr